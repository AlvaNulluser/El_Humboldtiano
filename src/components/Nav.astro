---
// Nav.astro — Barra de navegación con categorías editoriales
import { CATEGORIES } from "../utils/slugify";
---

<nav class="navegacion" aria-label="Navegación principal">
  <ul>
    {CATEGORIES.map((cat) => (
      <li><a href={`/categoria/${cat.slug}/1`}>{cat.name}</a></li>
    ))}
  </ul>
</nav>
